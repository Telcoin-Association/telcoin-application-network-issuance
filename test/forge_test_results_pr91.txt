# Verification record — weekly rewards pipeline (branch feat/tanip1-workbook-automation)

## Toolchain
$ forge --version
forge Version: 1.7.2-dev

## Solidity
$ forge build
Compiling 52 files with Solc 0.8.26 — success (style lint notes only, no errors)

$ forge test --match-contract 'RewardsNotifierTest|DeploymentsDecodeTest'
RewardsNotifierTest
  [PASS] test_constructor_grantsRolesAndBindsHistory()
  [PASS] test_constructor_revertsOnZeroAdmin()
  [PASS] test_constructor_revertsOnZeroHistory()
  [PASS] test_notify_emitsWhenEndBlockMatches()
  [PASS] test_notify_revertsForNonNotifier()
  [PASS] test_notify_revertsOnEndBlockMismatch()
DeploymentsDecodeTest
  [PASS] test_deploymentsJson_decodesWithoutFieldShift()
7 tests passed, 0 failed, 0 skipped

Full unit suite (--no-match-contract ForkTest): 10/11 —
testFuzz_increaseClaimableByBatch fails identically on the unmodified base
(OutOfGas at ~28k-entry batch from the unbounded uint16 fuzz input);
pre-existing and unrelated to this change set.

## TypeScript
$ yarn build   (plain tsc)
exit 0 — no errors

## Live chain verification (public Polygon RPC)
- getBlockByTimestamp reproduces the curated Wednesday boundaries exactly:
    jul-15 00:00 UTC -> 90245736 (== TANIP period 40 endBlock, curated periodStart)
    jul-22 00:00 UTC -> 90648935 (== TANIP period 41 endBlock, curated periodStart)
- resolveBlockRange correctly reports period 41 as merged-but-not-yet-settled
  (on-chain lastSettlementBlock 90245736 == period 40 end) and refuses period 42
  until settlement, which will start it contiguously at 90648936.
- resolveTelxRun targets period 50, READY, all 3 pools; period 50 passes arg
  parsing (PERIODS derived from checkpoints).
- TELx derived end block follows the historical convention end = boundaryBlock-1
  (period 49 ends 90648934; boundary 90648935 opens period 50).

## Last-full-epoch artifact regeneration (TANIP 41 / TELx 49, real committed data)
- build_workbook.py period 41: 95 stakers, all 4 gates pass, 40 prior periods
  intact, Cumulative 1107 wallets.
- safeTxArrayBuilder --tan 41: chunk sum 8,760,366 raw == source JSON total ==
  workbook Period Totals (87,603.66 TEL); 49 nonzero entries (46 wallets
  rebate-capped to zero are correctly omitted); notify params endBlock 90648935.
- safeTxArrayBuilder --telx 49: addRewards arrays produced for all 3 pools from
  the committed checkpoints.
- Not runnable outside CI: the raw calculator over a full epoch requires archive
  eth_getLogs + state (no public endpoint provides both). Covered by the one
  workflow_dispatch verification run with the repository's archive secrets.
